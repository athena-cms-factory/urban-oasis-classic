name: Deploy Athena Site

on:
  push:
    branches: [ main, 'feat/**', 'fix/**' ]
  repository_dispatch:
    types: [ publish_button ]

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install pnpm
        uses: pnpm/action-setup@v3
        with:
          version: 9

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install dependencies
        run: pnpm install --no-frozen-lockfile

      - name: Build project
        run: |
          if [ "${{ github.ref_name }}" = "main" ]; then
            pnpm build
          else
            # Build for preview subfolder
            REPO_NAME=$(echo "${{ github.repository }}" | cut -d'/' -f2)
            export VITE_BASE_URL="/$REPO_NAME/preview/"
            pnpm build --base="/$REPO_NAME/preview/"
          fi

      - name: Deploy to GitHub Pages (Main)
        if: github.ref_name == 'main'
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
          keep_files: true # Cruciaal om de preview map niet te verwijderen

      - name: Deploy to GitHub Pages (Preview Slot)
        if: github.ref_name != 'main'
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
          destination_dir: preview
          keep_files: true